<% content_for :title, "Log in — ShortURL" %>
<%= render "shared/navbar" %>

<div class="auth-wrapper">
  <div class="auth-card">
    <div class="auth-logo">
      <span class="nav-logo-icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24"
             fill="none" stroke="currentColor" stroke-width="2.5"
             stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71"/>
          <path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71"/>
        </svg>
      </span>
      ShortURL
    </div>

    <h1>Welcome back</h1>
    <p class="auth-subtitle">Log in to view your links and stats.</p>

    <% if flash.now[:alert] %>
      <div class="flash-error" role="alert">
        <svg xmlns="http://www.w3.org/2000/svg" width="15" height="15" viewBox="0 0 24 24"
             fill="none" stroke="currentColor" stroke-width="2"
             stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <circle cx="12" cy="12" r="10"/>
          <line x1="12" y1="8" x2="12" y2="12"/>
          <line x1="12" y1="16" x2="12.01" y2="16"/>
        </svg>
        <%= flash.now[:alert] %>
      </div>
    <% end %>

    <%= form_with url: login_path, class: "auth-form" do %>
      <div class="auth-field">
        <%= label_tag :email, "Email address" %>
        <%= email_field_tag :email, nil, class: "url-input",
            placeholder: "you@example.com", autofocus: true,
            autocomplete: "email" %>
      </div>
      <div class="auth-field">
        <%= label_tag :password, "Password" %>
        <%= password_field_tag :password, nil, class: "url-input",
            placeholder: "Your password",
            autocomplete: "current-password" %>
      </div>
      <%= submit_tag "Log in", class: "shorten-btn" %>
    <% end %>

    <p class="auth-switch">
      Don't have an account? <%= link_to "Sign up", signup_path %>
    </p>
  </div>
</div>
